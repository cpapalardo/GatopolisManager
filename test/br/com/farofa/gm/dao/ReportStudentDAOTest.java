package br.com.farofa.gm.dao;

import junit.framework.TestCase;

public class ReportStudentDAOTest extends TestCase {

	/*@Test
	public void testSave() {
		ReportStudentDAO dao = new ReportStudentDAOImpl(DataBaseManager.getEntityManager());
		Teacher teacher = new Teacher();
		teacher.setId(1);
		Student student = new Student();
		student.setId(1);
		ReportStudent rs = new ReportStudent(null, new Date(), 1, teacher, student);
		
		dao.save(rs);
		
		assertNotNull(rs);
		assertNotNull(rs.getId());
		
		ReportStudent expected = dao.findById(rs.getId());
		
		assertNotNull(expected);
		assertNotNull(expected.getId());
		
		assertEquals(expected.getDate_accessed(), rs.getDate_accessed());
		assertEquals(expected.getId(), rs.getId());
		assertEquals(expected.getView_count(), rs.getView_count());
	}

	@Test
	public void testUpdate() {
		fail("Not yet implemented");
	}

	@Test
	public void testDelete() {
		fail("Not yet implemented");
	}

	@Test
	public void testFindById() {
		fail("Not yet implemented");
	}

	@Test
	public void testFindByInep() {
		fail("Not yet implemented");
	}

	@Test
	public void testFindAll() {
		fail("Not yet implemented");
	}
*/
}
