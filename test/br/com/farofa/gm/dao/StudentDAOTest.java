package br.com.farofa.gm.dao;

import junit.framework.TestCase;

public class StudentDAOTest extends TestCase {

	/*@Test
	public void testSave() {
		StudentDAO dao = new StudentDAOImpl(DataBaseManager.getEntityManager());
		Room room = new Room();
		room.setId(1);
		Student student = new Student(null, "Aluno Teste", 'M', new Date(), 1,1,1,1, null, room);
		dao.save(student);
		
		Student expected = dao.findById(student.getId());
		
		assertNotNull(expected);
		
		assertNotNull(expected.getId());
		assertEquals(expected.getName(), student.getName());
		assertEquals(expected.getPicture_url(), student.getPicture_url());
		assertEquals(expected.getApp_rating(), student.getApp_rating());
		assertEquals(expected.getBirth_date(), student.getBirth_date());
		assertEquals(expected.getBuildings(), student.getBuildings());
		assertEquals(expected.getCoins(), student.getCoins());
		assertEquals(expected.getGender(), student.getGender());
		assertEquals(expected.getTime_in_city(), student.getTime_in_city());
	}

	@Test
	public void testUpdate() {
		fail("Not yet implemented");
	}

	@Test
	public void testDelete() {
		fail("Not yet implemented");
	}

	@Test
	public void testFindById() {
		fail("Not yet implemented");
	}

	@Test
	public void testFindByInep() {
		fail("Not yet implemented");
	}

	@Test
	public void testFindAll() {
		fail("Not yet implemented");
	}*/

}
